import { useEffect, useState } from 'react';

import './Voiture.scss';

const Voiture = () => {
  const [response, setResponse] = useState([]);

  return (
    <div>
    </div>
  );
};

export default Voiture;
